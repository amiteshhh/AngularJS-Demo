(function () {
    'use strict';

    /**
     * @ngdoc module
     * @module app.user
     * @name app.user
     * @packageName app.user
     * @area api
     * @description
     * module documentation to be adjusted for dgeni-alive documentation plugin 
     */
    var moduleName = 'app.user',
            requires = [];

    angular.module(moduleName, requires);

})();